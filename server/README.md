the server is coded in nodejs and to run it the dependencies must be installed 
	
		npm install

to start the server run following command : 

		npm start --PORT

	--PORT : have specify the port on which server should start eg. 

		npm start 3000
